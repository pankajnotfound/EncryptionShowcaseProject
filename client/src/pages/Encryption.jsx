import React from 'react';

const Encryption = () => {
  return (
    <div className="page-container">
      <h2 className="page-title">Encryption</h2>
      <p>Upload a file or enter a text message to encrypt using the selected security model.</p>
      {/* UI for the encryption process will go here */}
    </div>
  );
};

export default Encryption;
